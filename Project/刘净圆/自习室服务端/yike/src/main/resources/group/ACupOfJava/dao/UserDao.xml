<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="group.ACupOfJava.dao.UserDao">

    <select id="find" resultType="user">
        select * from user
    </select>
    <select id="talkList" resultType="group.ACupOfJava.pojo.Shop" parameterType="int">
        SELECT shop.* from user,shop,talk where user.user_id = talk.user_id and shop.shop_id = talk.shop_id and user.user_id = #{id}
    </select>


    <!--<select id="loginUser" parameterType="Map" resultType="User">
        select * from user where email = #{email} and password = #{password}
    </select>-->



</mapper>